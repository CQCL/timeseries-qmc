The quantum circuits and the sampled shots used for prodcuing the chains and the output are contained in the compressed
tar files `circuits.tar.gz` and `shots.tar.gz`.

The contents of these files can be extracted with the following commands:
tar -zxvf circuits.tar.gz
tar -zxvf shots.tar.gz